ssh-keygen -t rsa -b 4096 -C "ns465626@gmail.com"

eval "$(ssh-agent -s)"
# This will start the SSH agent in the background.

ssh-add ~/.ssh/id_rsa

cat ~/.ssh/id_rsa.pub
# Copy the output of the above command and add it to your GitHub account under Settings > SSH and GPG keys.

copy and paste the public key to your GitHub account
# Go to GitHub, navigate to Settings > SSH and GPG keys, and click on